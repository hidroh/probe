apply plugin: 'com.android.application'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        applicationId 'org.lucasr.probe.sample'
    }
}

dependencies {
    compile project(':library')
    provided project(':probe-gradle')
}
